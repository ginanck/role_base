---
# Apply kernel patches/updates for RHEL/CentOS (yum)

- name: Apply kernel updates (yum)
  ansible.builtin.yum:
    name: 'kernel*'
    state: latest  # noqa package-latest
  register: yum_kernel_update
