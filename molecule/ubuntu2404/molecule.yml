---
dependency:
  name: shell
  command: /usr/bin/bash meta/install_requirements
driver:
  name: docker
platforms:
  - name: role-base-ubuntu2404
    image: geerlingguy/docker-ubuntu2404-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    command: /lib/systemd/systemd
    tmpfs:
      - /run
      - /tmp
      - /run/lock
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        base_ca_install_enabled: false
        base_configure_cloud_init: false
        base_resolv_conf_managed: false
        base_hostname_configured: false
    host_vars:
      role-base-ubuntu2404:
        ansible_user: root
        base_hostname: "ubuntu-2404"
  playbooks:
    converge: ../resources/converge.yml
verifier:
  name: ansible
  options:
    playbook: ../resources/verify.yml
scenario:
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - create
    - prepare
    - converge
    - idempotence
    - side_effect
    - verify
    - cleanup
    - destroy
